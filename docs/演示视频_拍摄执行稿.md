# VelaPet 演示视频 · 拍摄执行稿(目标 ~4:40,≤5min)

口播为**逐字中文稿**,直接照读。拍法二选一:

---

## 🎬 推荐拍法:单机位「一镜到底」(最省事)

用**手机支架**固定手机,让**显示器(串口文字)+ 板子(双眼/摄像头/喇叭)同框**(见下图机位)——一个画面同时拍到"敲命令"和"板子当场反应"。这样能**跳过一大半流程**:

- ❌ 不用电脑录屏软件(GNOME/OBS 都不用)
- ❌ 不用把"屏录 + 板子实拍"两路在后期对齐/合成画中画
- ✅ **命令→反应在同一镜头里**,因果直观、更有说服力
- ✅ 一条音轨即可(手机麦同时收你的口播 + 喇叭应答"你好,我在")
- ✅ 后期只剩:**裁剪 + 给 `camera hr` 采样等待加速 + 打字幕 + 压到 ≤5min**(用电脑 kdenlive 或手机剪映/CapCut 都行)

**单机位 5 个要点**
1. **串口字看得清**:终端**字号调大 + 铺满屏**;显示器亮度适中(别泛光);关键行(`HR=60 good`、`MATCH`/`WAKE`)后期**打字幕**兜底。
2. **锁焦锁曝光**:开拍前点屏幕文字对焦,**锁定 AF/AE**,免得手伸去盖摄像头时画面来回对焦。
3. **防摩尔纹/反光**:手机尽量与屏幕**平行**、避开正对反光;有波纹就前后挪一点距离/角度。
4. **构图**:屏在上、板子在下,两者都入框;测心率时手指盖摄像头也在此框内完成。
5. **声音**:安静环境、口播清楚;先试录 5 秒回放,确认喇叭"你好,我在"能听清。

> 按 6 幕顺序一段段对着说、敲命令,基本一次拍完(某幕 NG 就单独重拍那一幕再拼)。
> 下面「录制操作」一节的电脑录屏方案作为**备选**——若嫌拍屏文字不够清,再改用电脑录串口 + 手机拍板子分拍。

---

## 一、开拍前 5 分钟自检清单

**环境 / 设备**
- [ ] 安静房间(命令词要现场说话);**光线稳定充足**(测心率靠环境光,别让光线忽明忽暗)。
- [ ] 板子固定放稳;串口连好:`fuser -k /dev/ttyUSB0; picocom -b 115200 /dev/ttyUSB0`。
- [ ] **终端字号调大**(方便视频看清);窗口拉宽,避免自动换行打乱输出。
- [ ] 固件是最新 `dev-ai` 编译烧录(含 `camera hr` / `kws` / `lcdtest`)。
- [ ] 手机/录屏软件就位;本稿口播词另开一屏或打印。
- [ ] **wifi/热点全程关着**(用来佐证"离线")。

**逐条复测(录之前先跑一遍确认稳)**
- [ ] `camera init` → `camera buf` → `camera hr`:手指轻盖摄像头、**保持静止**,末行出 `... good` 且 bpm 合理(≈55–90)。若出 `fair`/乱跳:调光、手指盖严一点、别压太紧,重试 2–3 次找到稳定手感。
- [ ] **麦克风用法(先记牢,决定成败)**:
      - **嘴离板载麦 ~8~12cm**——**别贴着麦**(贴近会削波 `peak=32767`,谱被打平、各词距离挤到一起分不开,必被拒识);太远(>20cm)音量又会掉到静音门附近偶发 `no speech`。
      - 每次 `enroll`/`run` 看新打印的 `[kws] level: peak=... rms=...`:**peak 落在 ~2000~15000** 就是甜点区;顶到 32767 就退远点,低于 300 就靠近点/说响点。
      - 正常偏大音量,把字**吐清楚**,尤其**最后一个字别吞**(如"查心率"的"率"稍微拖一下);**enroll 和 run 的距离/音量/念法保持一致**。
- [ ] **预先登记(录制时直接 run,省时间)**,含**大赛指定唤醒词**(采集窗口已放宽到 1.5s,不用赶):
      `kws clear`
      → `kws enroll nihaoopenvela`(说"你好openvela")×2
      → `kws enroll chaxinlv`(说"查心率",末字"率"吐清楚)×2
      → `kws enroll yundong`(说"开始运动")×2
      → `kws list`(应显示 6 条)。
      验证:说"你好openvela"→ `MATCH nihaoopenvela`;说命令词/无关话分别命中/拒识。
      > 登记与识别截同一段、经定长归一,只要**距离/音量/念法保持一致**即可稳定命中(实测正确词距离 ~7、异词 ~10+,阈值 11 有充足余量)。
- [ ] `kws run` 说"开始运动"→ `MATCH yundong`;说一句无关话 → `REJECTED`。各试 2 次确认稳。
- [ ] `lcdtest oeye`、`lcdtest blink`:双屏眼神/眨眼正常。
- [ ] **情感整合**用 `camera velapet`(已确认真驱动眼睛 + LED):初始困倦眼 → **拿起→睁眼 + 绿灯** → **摇动→开心 + 红绿双灯** → 低电时**红灯闪("饿了")**。拿起/摇动由加速度计驱动、不依赖摄像头,可靠。
      > 唯一注意:有人脸时 TRACK 态眼睛可能轻微抖动——拍摄时**让板子别正对人脸,只演拿起/摇动**即可。若仍想更稳,退回 `lcdtest oeye`(眼神)+ `lcdtest accel g`(手势串口打印)分开拍。
- [ ] `lcdtest bat`:读出电量数值。低电红灯闪烁**需电量真的低**——若现场造不出,**口播带过即可,不强拍**。
- [ ] `lcdtest led <pin> <0|1>`:确认你板子的 LED 引脚号(红灯常见 P40),点亮/熄灭各一次。

> 只要"测心率出 good、命令词说对能中说错能拒、眼神能亮"这三样稳,就可以开拍。其余(低电红灯/LED)拍到算加分,拍不到不卡。

---

## 录制操作(GNOME 自带录屏 + 手机分工)

**分工原则**
- 🖥️ **电脑录屏**(串口文字):`help`/`free`、`camera hr`、`kws` 输出、GitHub PR 页面 → 用 GNOME 自带录屏,文字清晰。
- 📱 **手机拍**(板子实物 + 声音):双屏眼神、手指盖摄像头、拿起/摇动、**喇叭说话("你好,我在""没听清")** → **电脑录屏不录声音**,所以语音应答一定靠手机收音!

**GNOME 录屏怎么用(零安装)**
- 开始/停止:**Ctrl + Alt + Shift + R**(顶栏出现红点=正在录;再按一次停)。
- 存到 `~/Videos/Screencasts/*.webm`。
- **每段单独录**(录前按开始、敲完/说完按停)→ 得到独立小段,好剪。
- 录之前把**终端字号调大、窗口铺满屏**;它**只录画面不录声**,口播/喇叭声后期用手机音轨或配音补。

**每幕录法对照**
| 幕 | 🖥️ 电脑录屏 | 📱 手机拍 |
|---|---|---|
| 0 开场 | — | 板子全貌 |
| 1 适配 | `help`/`free` 串口 | `lcdtest oeye` 双屏 |
| 2 测心率 | `camera hr` 串口 | 手指盖摄像头(同步) |
| 3 语音 | `kws` 串口 | 你说话 + **喇叭应答(收音关键)** |
| 4 拟人 | — | velapet 眼神/拿起/摇动/灯 |
| 5 收尾 | GitHub PR / `logs/` 页面 | — |

**顺序建议**:先把电脑录屏的几段一次录完(幕1/2/3/5),再用手机补板子实拍(幕0/1/2/3/4),最后用 kdenlive 对齐拼剪、加字幕、导出 ≤5 分钟。

---

## 二、拍摄执行稿(逐幕:画面 / 命令 / 口播 / 秒数)

> 口播语速按 ~3.5 字/秒;`camera hr` 采样等待用口播覆盖或剪辑加速。

### 幕 0 · 开场定位 —— 25s(0:00–0:25)
- **画面**:实拍板子全貌,双圆屏亮着眼神;叠标题字幕 "VelaPet · 跑在 BK7258 上的 openvela 端侧 AI 情感陪伴机器人"。
- **命令**:无(纯实拍空镜)。
- **口播**:
  > "这是 VelaPet,一台跑在博通集成 BK7258 上、基于小米 openvela 的桌面 AI 情感陪伴机器人。它最大的特点是:**完全离线、数据不出设备,断网也不变傻**。BK7258 是本届大赛官方的待适配开发板,我们完成了它的 openvela 首次移植。"

### 幕 1 · 适配成果(证明"底座硬")—— 40s(0:25–1:05)
- **画面**:屏幕录制串口;穿插实拍双屏点亮。`oeye` 会循环切换表情,串口打印 `NEUTRAL / HALF / SMOOTH BLINK / HAPPY`——可给这几个表情名打**中文字幕**(中性 / 半睁 / 眨眼 / 开心)。
  > **拍摄要点(重拍修正)**:手机端平、屏幕摆正、**拍全整行**——`Builtin Apps` 行的 `camera / kws / lcdtest / mic / psram` 五个命令要完整入框,`psram id` 的 `size = 16384 KB (16 MB)` 两行也要清晰,别贴边被切。终端窗口拉宽,避免右边跑出画面。
  > **标注用字幕、不要画框**:串口屏幕是斜的,正的框框和斜的文字对不齐、显糙;底部字幕与角度无关且和全片风格统一。两条字幕:①help/Builtin Apps 时→`已适配内置命令：camera / kws / lcdtest / mic / psram`;②psram id 时→`板载 16MB PSRAM（psram id 实测）`。
- **命令**(省时拍法:`help`、`free` **开录前先跑好、输出留在屏上**;`lcdtest oeye` **预先输好不回车**;开录后按 Enter 运行 oeye,一段口播把三样一起讲完):
  ```
  nsh> help          # 录前已跑,输出留屏(camera/kws/lcdtest/mic/psram)
  nsh> free          # 录前已跑,输出留屏(内存状态)
  nsh> psram id      # 录前已跑,输出留屏 → chip ID=0x8d08, size=16 MB(佐证 16MB PSRAM)
  nsh> lcdtest oeye  # 预先输好,开录按 Enter 运行 → 双屏切表情
  ```
  > 实测 `psram id` 打印 `size = 16384 KB (16 MB)`,与口播/字幕"16MB PSRAM"一致——画面里务必带上这两行。
- **口播**(一段连贯,按 Enter 运行 oeye 后边演边说):
  > "先看底座。屏幕上 `help` 列出的这些内置命令——**camera、kws、lcdtest、mic、psram**,都是我们把 BK7258 从零适配到 openvela 后、能实机跑的能力;`free` 显示系统带着这一整套功能常驻、依旧稳定运行,而真正吃内存的大块——摄像头帧缓冲、rPPG——都放在**板载 16 兆 PSRAM**(`psram id` 实测 16MB)里,端侧 AI 要用的内存都安排得下。"
  > (按 Enter 运行 `lcdtest oeye`,双屏亮起、开始切表情)"现在运行 `lcdtest oeye`——这就是它会传情的两只眼睛,在**中性、半睁、眨眼、开心**之间切换,屏上也同步打印这几种表情。从启动引导、UART 控制台,到双圆屏、摄像头、音频、加速度计、电量、16 兆 PSRAM,整套板级驱动都已开源合入大赛专属仓。"

### 幕 2 · 亮点一 · 摄像头指尖测心率(rPPG)—— 60s(1:05–2:05)
- **画面**:画中画——实拍手指轻盖摄像头,同屏串口输出;`sampling` 那 ~16 秒可 2–3 倍速。
- **命令**(省时拍法:`init`/`buf` **开录前先跑好留屏**,`camera hr` **预输好不回车**,开录按 Enter):
  ```
  nsh> camera init   # 录前先跑
  nsh> camera buf    # 录前先跑
  nsh> camera hr     # 预输好,开录按 Enter → 采样 ~16s 正好边等边讲原理
  ```
- **口播**(敲下 `camera hr` 后,趁采样这段讲):
  > "第一个亮点:这块板**没有心率传感器**,但我们用摄像头把心率算出来了。手指轻轻盖住镜头保持不动,摄像头在采集皮肤下血液流动引起的微小颜色变化,这就是 rPPG。它正在采 450 帧、大约十几秒,做带通滤波和自相关,分三个窗口交叉验证。"
  > (出结果后)"看,HR 60 bpm,三个窗口一致、判定 good——**没有传感器,硬是用一颗摄像头量出了心率**,难点和创新都在这里。"

### 幕 3 · 亮点二 · 离线语音:唤醒 + 应答(自研 MFCC+DTW)—— 90s(2:05–3:35)
- **画面**:屏幕录制 `kws` 串口 + 实拍你对着板子说话、**喇叭出声(手机收音!)**;给"wifi/热点已关闭"一个 1–2 秒特写。
- **⚠️ 说话距离(本幕成败关键)**:**嘴离板载麦 ~8~12cm,别贴着麦**(贴近削波必翻车);末字吐清楚。串口那行 `level: peak=...` 落在 ~2000~15000 即稳。若某次被拒,退远一点、把最后一个字说清再来一遍即可,别慌。
- **命令**(唤醒词/命令词已预先登记;省时拍法:`kws list` 留屏,`kws wake`/`kws run` **逐条预输好不回车**,按 Enter 后再说话/边演边说):
  ```
  nsh> kws list                 # 你好openvela / 查心率 / 开始运动
  nsh> kws wake   ← 说“你好openvela”  → 喇叭答“你好,我在”
  nsh> kws run    ← 说“查心率”         → 命中
  nsh> kws run    ← 说“开始运动”       → 命中
  nsh> kws run    ← 说一句无关的话      → 拒识
  nsh> kws wake   ← 故意小声/含糊       → 喇叭答“没听清,请再说一遍”
  ```
- **口播**:
  > "第二个亮点是离线语音,而且是**纯自研的 MFCC 加 DTW,一行第三方库都没有,断网也能用**。先看最关键的——**唤醒加应答**,注意此刻板子是**断网**的。"
  > (`kws wake`,清晰说"你好 openvela",等喇叭回话)------"你好 openvela——(喇叭:'你好,我在')你听,它不光听懂了,还**开口回你**。用的正是大赛统一指定的唤醒词。"
  > (`kws wake`,故意含糊)"------没听清它也会应你——(喇叭:'没听清,请再说一遍')。"
  > (`kws run`,说"查心率")"------再来命令词——'查心率',命中;它就能去触发刚才那个摄像头测心率。"
  > (`kws run`,说"开始运动")"------'开始运动'——也命中,和其它词距离拉得很开。"
  > (`kws run`,说无关话)"------说句无关的——直接拒识,不误报。"

  > "从采音、算 MFCC、到模板匹配和语音回应,**整条链路全在板子上,零延迟、不联网**。别人的方案要连云、网一慢就转圈——**我们断网也能听、能答**。"

### 幕 4 · 亮点三 · 拟人交互 + 情感闭环 —— 50s(3:35–4:25)
- **画面**:实拍为主。`camera velapet` 后:**拿起→睁眼 + 绿灯**,**摇一摇→开心 + 红绿双灯**(低电还会红灯闪)。板子**别正对人脸**,避免跟踪态眼睛抖。
- **命令**(省时拍法:`camera velapet` **预输好不回车**,开录按 Enter,然后拿起/摇动边演边讲):
  ```
  nsh> camera velapet        # 预输好,开录按 Enter;整合:眼神 + 手势 + LED(演完按 Enter 退出)
  ```
  > 已确认 velapet 真驱动眼睛(EYE_EXPR_WAKE/HAPPY)与 LED(P40 红/P41 绿);拿起/摇动由加速度计驱动,不依赖摄像头。**velapet 一个命令就整合了 眼神 + 拿起/摇动 + LED,幕4 只跑它即可,不用再单独 `lcdtest oeye`(幕1 已展示过表情)。**
  > **仅在翻车时用的兜底**:若 velapet 的拿起/摇动眼神反应在镜头里不好拍,可用 `lcdtest accel g`(串口打印手势判定)证明"手势确实被识别到";正常能拍到眼神反应就用不上它。
  > **关于眨眼**:眼睛会周期性眨(TRACK 态约每 50 帧、SLEEP 态约每 100 帧)是**有意的拟人动画,不是卡顿/bug**——口播里点一句,评委才不会误会。
  > **人脸跟随(TRACK 态瞳孔跟脸)本次不展示**:功能已实现(亦见独立命令 `camera track`),但肤色检测受光照/白平衡影响、眼睛会抖,未达稳定演示标准,故让板子别正对人脸;作为后续优化项(更值得展示,但需先把检测做稳)。
- **口播**:
  > "光会算还不够,得有'陪伴感'。双圆屏是它的眼睛,**你看它还会时不时眨眼、像真的在看着你——这是特意做的拟人动画**。板载加速度计让它有体感——**把它拿起来,它就醒过来;摇一摇,它会开心、亮起灯**。它还能读自己的电量,快没电了就像'饿了'一样提醒你。视觉、体感、声光一起,把情绪外化出来。"

### 幕 4.5 · 工程深度:功耗实测 + 能效(补拍)—— ~25s(4:25–4:50)
- **画面**:实拍 **UT181A 万用表串在电池线上**读数(拍清数字);建议拍**两个状态对比**——
  ① `camera velapet` 运行时,表显 **~135mA**;② 复位后待机(不跑命令),表显 **~16mA**。
  可穿插串口 `camera pwrdump`(寄存器下电验证)或 `lcdtest bat` 一两秒特写。
- **命令**(可选,留屏即可):
  ```
  nsh> camera pwrdump   # 逐寄存器 dump(佐证外设退出已下电)
  nsh> lcdtest bat      # 读电量
  ```
- **口播**(~25s,对着万用表读数说;**数字须与画面读数一致**):
  > "能跑,还得**省电、跑得动**。我们用万用表串在电池上逐档实测:**真待机只有 14 毫安**,一块 1000 毫安时电池能**待机约三天**;满载跑摄像头加双屏也有**约七个半小时续航**。这些都不是估的——我们自研了**寄存器级的功耗排查命令**,把待机从近 100 毫安一路压到十几毫安。性能上,测心率、离线语音、双屏眼神全是**实时跑通**的。"
  > ⚠ 若补拍时读数又略有不同(如 15/135),照读画面上的实际数字即可——口播/字幕跟实拍读数一致最重要。
- **字幕**(叠在读数镜头上,数字对齐画面):
  - `真待机 14mA · 1000mAh ≈ 3 天`
  - `velapet 满载 ~134mA · 续航 ≈ 7.5h`
  - `逐寄存器实测:待机 ~100mA → 14mA`
- **拍摄要点**:万用表数字要清晰入框;两个状态各停 2~3 秒;`pwrdump` 那屏字太多可只给 1~2 秒特写、靠字幕点题。

> **性能字幕(无需补拍,后期叠在已拍的幕2/3/4 上即可)**:
> - 幕2 测心率画面叠 `rPPG 450帧@27.7fps · 三窗一致 good`;
> - 幕3 语音画面叠 `离线 MFCC+DTW · 1.5s窗 · 命中率94%`;
> - 幕4 眼神画面叠 `双屏眼神实时 · 取最新帧节流不卡顿`。
> 这样"性能"在原有画面上用字幕点出数值,不用额外补拍。

### 幕 5 · 收尾(合规 + 生态)—— 20s(4:50–5:00)
- **画面**:屏幕录制 GitHub 已合入的 PR 列表(#31 摄像头心率、#33 离线命令词、日志)+ `logs/` 目录。
- **命令**:无(网页/文件截图)。
- **口播**:
  > "全程 AI 辅助开发、对话日志可查,代码全部开源合入,回馈 openvela 社区。我们也如实标注了已知限制。VelaPet——**在一块全新的国产 AIoT 芯片上,做出一个断网也不变傻、会看你、会关心你的 AI 伙伴**。谢谢!"

---

## 三、剪辑与注意事项
- **总时长**:上面合计约 4:45(幕3 唤醒+两个命令词+应答),留缓冲,务必 ≤5:00。
- **加速**:`camera hr` 的采样等待、KWS/mic 命令的麦克风 init 刷屏日志,剪辑时 2–4 倍速或跳剪。
- **字幕**:关键数值打字幕强调(`HR = 60 bpm good`、`MATCH yundong 3.84`、`REJECTED`)。
- **离线佐证**:开头或幕 3 给"wifi 关闭/热点关闭"一个 1–2 秒特写,强化"断网可用"。
- **声音**:命令词那段现场收音要清楚;其余口播可后期配音,避免风扇/键盘杂音。
- **兜底**:低电红灯、LED 若没拍到,用口播一句带过即可,别为它反复重录。
- **片尾**:可留一帧 repo 地址 / 队伍名,方便评委记录。
